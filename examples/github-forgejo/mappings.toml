# Auto-generated by Shotgun, then hand-edited (see `edited = true` entries).
# Source: examples/github-forgejo/github-api.json
# Target: examples/github-forgejo/forgejo-api.json
# Generated: 2026-08-19T01:01:13.102187+00:00
# Coverage: 4/5 endpoints mapped (80%)

[meta]
source_spec = "examples/github-forgejo/github-api.json"
target_spec = "examples/github-forgejo/forgejo-api.json"
generated_at = "2026-08-19T01:01:13.102187+00:00"
shotgun_version = "0.1.0"

[settings]
target_base_path = "/api/v1"
unmapped_endpoint_behavior = "reject"
unmapped_field_behavior = "passthrough"

[settings.pagination]
source_style = "link_header"
target_style = "link_header"
rewrite_link_urls = true

[settings.pagination.param_map]
per_page = "limit"

[[endpoints]]
source = "GET /repos/{owner}/{repo}"
target = "GET /repos/{owner}/{repo}"
# Hand-edited: "homepage" and "website" are the same field under different
# names. Auto-diff won't guess this -- it only auto-maps exact name matches
# and lists everything else as a default/drop for a human to reconcile.
edited = true

[endpoints.response]
drops = ["internal_tracker"]

[endpoints.response.renames]
homepage = "website"

[endpoints.response.defaults]
node_id = ""
topics = []

[[endpoints.response.nested]]
path = "owner"
schema_map = "User"

# No target endpoint for GitHub Actions in this subset -- Forgejo has no
# equivalent concept here. Left unmapped; 501s until this is addressed.
[[endpoints]]
source = "GET /repos/{owner}/{repo}/actions/runs"
target = ""
note = "no matching path or operationId found in target spec"

# Hand-edited: GitHub's "per_page" and Forgejo's "limit" are the same query
# param under a different name -- not something the deterministic matcher
# will guess.
[[endpoints]]
source = "GET /repos/{owner}/{repo}/commits"
target = "GET /repos/{owner}/{repo}/commits"
edited = true

[endpoints.query_params]
per_page = "limit"

[[endpoints]]
source = "GET /user"
target = "GET /user"
edited = true

[endpoints.response]
drops = []

[endpoints.response.renames]
created_at = "created"
name = "full_name"

[endpoints.response.defaults]
node_id = ""
two_factor_authentication = false

# Hand-edited: auto-diff correctly refused to guess this one -- the path
# matches (modulo the {pull_number}/{index} param name) but the HTTP method
# differs (GitHub PUT vs Forgejo POST), which is exactly the kind of thing
# that needs a human to confirm before it's wired up.
[[endpoints]]
source = "PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge"
target = "POST /repos/{owner}/{repo}/pulls/{index}/merge"
edited = true

[endpoints.path_params]
pull_number = "index"

[endpoints.response.defaults]
message = ""

[[schemas]]
name = "User"
edited = true

[schemas.renames]
created_at = "created"
name = "full_name"

[schemas.defaults]
node_id = ""
two_factor_authentication = false
