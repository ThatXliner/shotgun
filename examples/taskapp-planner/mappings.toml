# Auto-generated by Shotgun, then hand-edited (see `edited = true` entries).
# Source: examples/taskapp-planner/taskapp-api.json
# Target: examples/taskapp-planner/planner-api.json
# Coverage: 3/4 endpoints mapped (75%)

[meta]
source_spec = "examples/taskapp-planner/taskapp-api.json"
target_spec = "examples/taskapp-planner/planner-api.json"
generated_at = "2026-08-19T01:01:13.102187+00:00"
shotgun_version = "0.1.0"

[settings]
target_base_path = "/v2"
unmapped_endpoint_behavior = "reject"
unmapped_field_behavior = "passthrough"

[settings.pagination]
source_style = "link_header"
target_style = "link_header"
rewrite_link_urls = true

[settings.pagination.param_map]
per_page = "limit"

# Hand-edited: "title"/"name" and "due_date"/"deadline" are the same fields
# under different names. Auto-diff listed them as defaults/drops; converted
# to proper renames.
[[endpoints]]
source = "GET /tasks/{task_id}"
target = "GET /items/{item_id}"
edited = true

[endpoints.path_params]
task_id = "item_id"

[endpoints.response]
drops = ["internal_notes"]

[endpoints.response.renames]
title = "name"
due_date = "deadline"

[endpoints.response.defaults]
starred = false
tags = []

[endpoints.response.type_conflicts]
priority = "integer vs string"

[[endpoints.response.nested]]
path = "assignee"
schema_map = "User"

# Listing endpoint with query param remapping.
[[endpoints]]
source = "GET /tasks"
target = "GET /items"
edited = true

[endpoints.query_params]
per_page = "limit"
sort_by = "order"

# No target equivalent — Planner has no reminder concept.
[[endpoints]]
source = "POST /tasks/{task_id}/remind"
target = ""
note = "Planner has no reminder concept"

# Hand-edited: auto-diff matched the path but refused the method change
# (PUT vs PATCH). Confirmed by hand.
[[endpoints]]
source = "PUT /tasks/{task_id}"
target = "PATCH /items/{item_id}"
edited = true

[endpoints.path_params]
task_id = "item_id"

[endpoints.request.renames]
title = "name"
due_date = "deadline"

[[schemas]]
name = "User"
edited = true

[schemas.renames]
display_name = "full_name"

[schemas.defaults]
avatar_url = ""
